# Backup and copy files
*.kopie
* - Kopie.*
*.bak
*.backup
*~

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/settings.json
.idea/

# Python
.venv/

# Temporary files
*.tmp
*.log

# Supervisor state backup – contains access/refresh/ingress tokens, never commit.
# Persisted to Windows via the workspace bind mount, not via git.
.supervisor-state-backup/
.mcp.json











.claude/settings.json
